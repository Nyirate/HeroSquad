{{#partial "content"}}

    <h1></h1>

    {{#if posts}}
        {{#each posts}}
            {{name}}
            {{age}}
            {{power}}
            {{weakness}}
        {{/each}}
    {{else}}
        <p>No hero added</p>
    {{/if}}

{{/partial}}

{{> layout.hbs}}
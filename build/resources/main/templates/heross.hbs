{{#partial "content"}}

    <h1></h1>

    {{#if posts}}
        {{#each posts}}
            {{name}}
            {{age}}
            {{power}}
            {{weakness}}
        {{/each}}
    {{/if}}

{{/partial}}

{{> layout.hbs}}
{{#partial "content"}}

    <h1></h1>

    {{#if squads}}
        {{#each squads}}
            {{name}}
            {{size}}
            {{cause}}
        {{/each}}
    {{else}}
        <p>No squad added</p>
    {{/if}}

{{/partial}}

{{> layout.hbs}}